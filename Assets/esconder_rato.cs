using UnityEngine;

public class esconder_rato : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Cursor.lockState = CursorLockMode.Locked; // Esconde o cursor e o trava no meio da tela
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
